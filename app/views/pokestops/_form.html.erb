<%= form_for(@pokestop) do |f| %>
  <p>If you know the name of the stop, please add it. If not, it is okay! Drag the pin below or press on the map to indicate where the stop is.</p>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <%= f.hidden_field :latitude, id: "placemarker-lat" %>
  <%= f.hidden_field :longitude, id: "placemarker-lng" %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
